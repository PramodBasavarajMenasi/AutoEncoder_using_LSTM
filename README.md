# AutoEncoder_using_LSTM
LSTM autoencoder trained on MNIST by treating each image as a sequence. I observed that removing activation functions improved reconstruction quality due to MNIST’s simple, linear structure.
